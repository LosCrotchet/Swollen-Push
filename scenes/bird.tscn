[gd_scene format=3 uid="uid://dayqy17dkna82"]

[ext_resource type="Script" uid="uid://buqyesgqag578" path="res://scenes/gd/bird.gd" id="1_3po83"]
[ext_resource type="SpriteFrames" uid="uid://bk22vwxxswhqe" path="res://assets/tres/bird_frames.tres" id="1_kqhkv"]
[ext_resource type="Texture2D" uid="uid://0i1bludgdjr8" path="res://assets/textures.png" id="5_b7mtg"]

[sub_resource type="CircleShape2D" id="CircleShape2D_twsrt"]
radius = 50.0

[sub_resource type="Shader" id="Shader_hq4t0"]
code = "shader_type canvas_item;

// 阴影颜色与透明度
uniform vec4 shadow_color : source_color = vec4(0.0, 0.0, 0.0, 0.4);

// 光源角度（度数，0代表右，90代表下）
instance uniform float shadow_angle : hint_range(0.0, 360.0) = 45.0;

// 阴影拉伸倍数（1.0为原始大小，数值越大影子被拉得越长）
uniform float shadow_length : hint_range(1.0, 5.0) = 1.5;

// 小鸟距离地面的高度（控制阴影的整体偏移距离）
uniform float shadow_height : hint_range(0.0, 200.0) = 30.0;

// 错切/斜切因子（用于模拟不同视角的3D透视形变，0为无错切）
uniform float skew_factor : hint_range(-2.0, 2.0) = 0.0;

void vertex() {
    // 将角度转换为弧度，并计算光线的方向向量
    float rad = radians(shadow_angle);
    vec2 light_dir = vec2(cos(rad), sin(rad));
    
    // 1. 沿光线方向的拉伸变换 (Scale along light axis)
    // 利用点积算出当前顶点在光线方向上的投影分量
    float dot_prod = VERTEX.x * light_dir.x + VERTEX.y * light_dir.y;
    vec2 stretched_vertex = VERTEX + light_dir * dot_prod * (shadow_length - 1.0);
    
    // 2. 垂直于光线方向的错切变换 (Skew)
    // 计算出法线向量，用于产生不对称的倾斜感
    vec2 normal_dir = vec2(-light_dir.y, light_dir.x);
    float normal_dot = VERTEX.x * normal_dir.x + VERTEX.y * normal_dir.y;
    stretched_vertex += light_dir * normal_dot * skew_factor;
    
    // 3. 应用高度偏移 (Offset)
    // 影子根据光源方向投射出一段距离，表现出小鸟悬空的高度
    VERTEX = stretched_vertex + light_dir * shadow_height;
}

void fragment() {
    // 采样原图的像素
    vec4 tex_color = texture(TEXTURE, UV);
    
    // 丢弃原图的色彩，只保留透明度 (Alpha)
    // 将其乘以我们自定义的阴影透明度
    COLOR = vec4(shadow_color.rgb, tex_color.a * shadow_color.a);
}"

[sub_resource type="ShaderMaterial" id="ShaderMaterial_b7mtg"]
shader = SubResource("Shader_hq4t0")
shader_parameter/shadow_color = Color(0, 0, 0, 0.4)
shader_parameter/shadow_length = 2.8690000887775
shader_parameter/shadow_height = 200.0
shader_parameter/skew_factor = 0.5090001191775002

[sub_resource type="Shader" id="Shader_twsrt"]
code = "shader_type canvas_item;

instance uniform bool enable = true;
uniform vec4 outline_color : source_color = vec4(1.0, 1.0, 1.0, 1.0);
uniform float width : hint_range(0.0, 10.0) = 1.0;
uniform int pattern : hint_range(0, 2) = 0; // 0: 菱形, 1: 圆形, 2: 方形

void fragment() {
    float size = width * fwidth(UV.x); // 适配缩放
    vec2 unit = TEXTURE_PIXEL_SIZE;
    float alpha = 0.0;
    
    if (pattern == 0) {
        // 菱形采样 (性能最好)
        alpha += texture(TEXTURE, UV + vec2(0.0, -width) * unit).a;
        alpha += texture(TEXTURE, UV + vec2(0.0, width) * unit).a;
        alpha += texture(TEXTURE, UV + vec2(-width, 0.0) * unit).a;
        alpha += texture(TEXTURE, UV + vec2(width, 0.0) * unit).a;
    } else {
        // 圆形采样 (效果最圆润)
        for (float x = -width; x <= width; x++) {
            for (float y = -width; y <= width; y++) {
                if (pattern == 1 && length(vec2(x, y)) > width) continue;
                alpha += texture(TEXTURE, UV + vec2(x, y) * unit).a;
            }
        }
    }
    
    vec4 col = texture(TEXTURE, UV);
    // 如果原像素透明且周围有像素，则显示描边
    if (enable && col.a < 0.1 && alpha > 0.0) {
        COLOR = outline_color;
    } else {
        COLOR = col;
    }
	
	
}"

[sub_resource type="ShaderMaterial" id="ShaderMaterial_twsrt"]
shader = SubResource("Shader_twsrt")
shader_parameter/outline_color = Color(1, 1, 0, 1)
shader_parameter/width = 10.0
shader_parameter/pattern = 1

[node name="Bird" type="Node2D" unique_id=2113065109]
script = ExtResource("1_3po83")

[node name="Area2D" type="Area2D" parent="." unique_id=388157875]

[node name="CollisionShape2D" type="CollisionShape2D" parent="Area2D" unique_id=516558471]
shape = SubResource("CircleShape2D_twsrt")

[node name="AnimatedOutlookShadow" type="AnimatedSprite2D" parent="." unique_id=652445945]
material = SubResource("ShaderMaterial_b7mtg")
scale = Vector2(0.5, 0.5)
sprite_frames = ExtResource("1_kqhkv")
animation = &"normal_walk"

[node name="AnimatedOutlook" type="AnimatedSprite2D" parent="." unique_id=1489001230]
material = SubResource("ShaderMaterial_twsrt")
scale = Vector2(0.5, 0.5)
sprite_frames = ExtResource("1_kqhkv")
animation = &"fixed_walk"

[node name="Outlook" type="Sprite2D" parent="." unique_id=1724399304]
texture_filter = 1
texture = ExtResource("5_b7mtg")
region_enabled = true
region_rect = Rect2(0, 192, 64, 64)

[connection signal="input_event" from="Area2D" to="." method="_on_area_2d_input_event"]
[connection signal="mouse_entered" from="Area2D" to="." method="_on_area_2d_mouse_entered"]
[connection signal="mouse_exited" from="Area2D" to="." method="_on_area_2d_mouse_exited"]
